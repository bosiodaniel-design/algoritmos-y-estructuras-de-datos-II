# Trabajo Práctico I

## Alumno

Daniel Bosio

## Número de trabajo práctico

TP N° 1

## Descripción

* Ejercicio 1: creo la Clase Vehiculo con herencia a Auto y Moto usando super() y método describir.
Primero creo la clase (class Vehiculo():
defino sus atributos (marca, velocidad max) creo la funcion describir(me muestra marca y velocidad final)
creo clase hija Auto y Moto que heredan de la clase padre los atributos con super().init (marca, velocidad max)
ambas con la funcion describir(muestra marca y velocidad_max) pero sobreescribiendo los atributos.
creo los objetos auto y moto con sus atributos de la class Auto


* Ejercicio 2: creo la clase Figura con los atributos (color) con un metodo area().
  Clases hijas Rectangulo que hereda clor de clase padre mas atributos base y altura.
  La otra clase hija con radiio (tambien hereda color) ambas clases hijas contienen un metodo que calcula el    area de ambas figuras.
  se agrega el metodo describir en la clase padre.
  Creacion de los objetos, una lista y un for para recorrer mostrando color y area de las figuras.
 

## Instrucciones para ejecutar

1. Descargar el repositorio
2. Abrir una terminal en la carpeta trabajo-practico-I
3. Ejecutar:
   python tp1_ejercicio1.py
   python tp1_ejercicio2.py
   
